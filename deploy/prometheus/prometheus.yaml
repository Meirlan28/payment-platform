global:
  scrape_interval: 15s
  evaluation_interval: 10s
rule_files:
  - /etc/prometheus/alerts.yaml
scrape_configs:
  - job_name: payment-api
    static_configs:
      - targets: [payment-api:9090]

