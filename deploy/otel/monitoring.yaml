apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: otel-gateway
  namespace: observability
  labels:
    monitoring: platform
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: otel-gateway
  endpoints:
    - port: prometheus
      interval: 15s
      scrapeTimeout: 10s

